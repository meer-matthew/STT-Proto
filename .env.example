# Environment Configuration Template
# Copy this file to create your own environment files:
#   - .env.development (for local testing)
#   - .env.local (for live testing)
#   - .env.production (for production builds)

# ============================================
# Backend Configuration
# ============================================

# Local Backend (for development with local machine)
# Run "npm run detect-ip" to auto-detect your machine's IP
# RENDER_APP_URL=http://192.168.68.101:5001

# Live Render Backend (for testing production backend)
# RENDER_APP_URL=https://stt-proto-1.onrender.com

# Legacy Heroku URL (still supported for backward compatibility)
# HEROKU_APP_URL=https://your-app-name.herokuapp.com

# ============================================
# App Configuration
# ============================================

# Environment: development | staging | production
NODE_ENV=development

# Enable debug logging
DEBUG=true

# API debug (shows all API requests/responses)
API_DEBUG=false

# ============================================
# Feature Flags
# ============================================

# Enable experimental features
ENABLE_EXPERIMENTAL=false

# Voice provider: deepgram | openai | anthropic
VOICE_PROVIDER=deepgram

# ============================================
# Notes
# ============================================
# - .env.development: Local testing against your local backend
# - .env.local: Local testing against the live Render backend
# - .env.production: For production builds (set during build time)
# - Don't commit API keys to version control!
